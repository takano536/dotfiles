if type -q eza
    alias ls "eza --icons"
end

alias ll "ls -al --classify=always"
alias la "ls -a"
alias l "ls -l1 --classify=always"

# nvim
if type -q nvim
    alias vi "nvim"
    alias vim "nvim"
end
